{
    "routes": [
        {
            "route": "/profile",
            "allowedRoles": ["admin", "contributor", "custom", "reader", "authenticated"]
        },
        {
            "route": "/dashboard",
            "allowedRoles": ["admin"]
        }
    ]
}
